{
  "name": "bilbase-api-tests",
  "version": "1.0.0",
  "description": "Playwright API tests for Bilbase backend",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
