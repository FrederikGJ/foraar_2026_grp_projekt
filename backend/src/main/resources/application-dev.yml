spring:
  datasource:
    url: jdbc:mysql://localhost:3310/bilbase?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: user
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.springframework.web: INFO
